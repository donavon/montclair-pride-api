# --- Node / Dependencies ---
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Environment Secrets ---
.env
.env.local
.env.*.local

# --- Claude Code Memory Protocol ---
# Ignore the root of plans (scratchpads), but keep the completed folder
/plans/*
!/plans/completed/

# --- Claude Code ---
.claude/*
!.claude/commands/

# --- OS / IDE ---
.DS_Store
.vscode/
.idea/

# --- Cloudflare ---
worker-configuration.d.ts
.wrangler
.dev.vars
dist/
.cache/

# Terraform local state and temporary files
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars
terraform/*.tfvars.json

# Terraform crash logs
terraform/crash.log
terraform/crash.*.log

# Terraform provider plugins
terraform/*.exe
terraform/terraform.tfstate.d/

# Terraform lock file 
# (Optional: some prefer to check this in to lock provider versions, 
# but for solo projects, it's often ignored)
# terraform/.terraform.lock.hcl
